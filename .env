# Banco de dados (será configurado no Railway)
DATABASE_URL=postgresql://username:password@host:port/database

# JWT Secret (GERAR UMA CHAVE SEGURA)
JWT_SECRET=seu_jwt_secret_super_seguro_aqui_altere_isto

# SendGrid API Key (para e-mails reais)
SENDGRID_API_KEY=sua_chave_sendgrid_aqui

# Ambiente
NODE_ENV=production
